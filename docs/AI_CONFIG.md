# AI 配置说明

为了启用 AI 互动功能，请填写本地配置文件：

`config/ai.json`

字段说明：
- `baseUrl`: ChatGPT API 的基础地址（例如 `https://api.openai.com`）
- `apiKey`: 你的 API Key
- `model`: 模型名称（默认 `gpt-4o-mini`）

注意：
- 这是本地开发使用的配置文件，请勿提交真实密钥到公开仓库。
